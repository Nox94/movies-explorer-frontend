import '../../common.css';
import './register.css';
import Form from "../Form/Form.js";

function Register() {
    return (
        <section className="register section">
            <Form/>
        </section>
    )
}

export default Register;
